import type {PositionArray} from './Position'
export type AnimalInfo = {
  id: string
  name: string
  modelPath: string
  modelScale: number
  position: PositionArray
  floorColor: string
  wallColor: string
  videoId: string
}
